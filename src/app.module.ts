import { Module } from '@nestjs/common';

@Module({
  imports: [
    // Aquí importaremos los módulos: PosModule, InventoryModule, etc.
  ],
  controllers: [],
  providers: [],
})
export class AppModule {}
